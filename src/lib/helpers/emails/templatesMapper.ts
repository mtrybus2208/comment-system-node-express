import { TemplatesMapper } from '../../../types/helpers/emails/emails.types';

const templatesMapper: TemplatesMapper = {
  passwordReset: 'templates/email/passwordReset.template.mjml',
  information: 'templates/email/information.template.mjml',
};

export default templatesMapper;
